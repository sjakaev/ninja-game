# 📝 ИНСТРУКЦИЯ ПО ДЕПЛОЮ

## ✅ ЧТО НУЖНО СДЕЛАТЬ

### 1. Создай репозиторий на GitHub

1. Зайди на https://github.com/new
2. Название: `ninja-game` (или любое другое)
3. Сделай репозиторий **PUBLIC**
4. НЕ добавляй README, .gitignore, license (у нас уже есть)
5. Нажми **Create repository**

### 2. Скопируй команды которые GitHub покажет

GitHub покажет что-то типа:

```bash
git remote add origin https://github.com/ТВОЙ-ЮЗЕРНЕЙМ/ninja-game.git
git branch -M main
git push -u origin main
```

**НО СНАЧАЛА** нужно настроить проект!

### 3. Настрой проект ПЕРЕД пушем

#### 3.1 Открой `vite.config.js`

Замени эту строку:
```javascript
base: '/ninja-game/', 
```

На название ТВОЕГО репозитория:
```javascript
base: '/ТВОЕ-НАЗВАНИЕ-РЕПО/', 
```

Например, если репо называется `cool-ninja-game`:
```javascript
base: '/cool-ninja-game/', 
```

#### 3.2 Сохрани изменения

### 4. Залей на GitHub

Теперь выполни команды из терминала в папке проекта:

```bash
# Инициализируй git
git init

# Добавь все файлы
git add .

# Создай первый коммит
git commit -m "🎮 Initial commit - Ninja Chase Game"

# Подключи свой репозиторий (замени на свой URL!)
git remote add origin https://github.com/ТВОЙ-ЮЗЕРНЕЙМ/ТВОЙ-РЕПО.git

# Переименуй ветку в main
git branch -M main

# Запуш на GitHub
git push -u origin main
```

### 5. Включи GitHub Pages

1. Зайди в свой репозиторий на GitHub
2. Нажми **Settings** (вкладка справа)
3. В меню слева найди **Pages**
4. В секции **Source** выбери: **GitHub Actions**
5. Всё! Настройка завершена.

### 6. Деплой начнётся автоматически!

1. Зайди во вкладку **Actions** в репозитории
2. Увидишь процесс деплоя (жёлтый кружок = в процессе, зелёная галка = готово)
3. Подожди 1-2 минуты

### 7. Открой игру! 🎉

Твоя игра будет доступна по адресу:

```
https://ТВОЙ-ЮЗЕРНЕЙМ.github.io/ТВОЙ-РЕПО/
```

Например:
```
https://romandev.github.io/ninja-game/
```

## 🔄 Обновление игры

Когда захочешь что-то изменить:

```bash
# Внеси изменения в коде

# Добавь изменения
git add .

# Создай коммит
git commit -m "✨ Описание изменений"

# Запуш
git push
```

GitHub Actions автоматически пересоберёт и задеплоит! ✨

## ❓ Проблемы?

### Игра не загружается (белый экран)

**Решение:** Проверь `vite.config.js` - параметр `base` должен совпадать с названием репозитория!

### Actions упал с ошибкой

**Решение:** Зайди в Settings → Pages → убедись что Source = **GitHub Actions**

### PeerJS не работает

**Решение:** PeerJS может иметь проблемы с некоторыми сетями. Попробуй:
- Другой WiFi
- Мобильный интернет
- VPN

## 🎮 Готово!

Теперь можешь кидать друзьям ссылку и играть онлайн! 🚀
